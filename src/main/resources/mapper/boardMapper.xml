<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BoardMapper">

    <!-- 게시글 등록 -->
    <insert id="insert" parameterType="com.buybike.app.domain.Board" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO board (memberId, title, content, regDt)
        VALUES (#{memberId}, #{title}, #{content},  NOW())
    </insert>

    <!-- 전체 게시글 조회 -->
    <select id="findAll" resultType="com.buybike.app.domain.Board">
        SELECT boardId, memberId,  title, content, hit, regDt
        FROM board
        ORDER BY id DESC
    </select>

    <!-- 게시글 단건 조회 -->
    <select id="findById" parameterType="long" resultType="com.buybike.app.domain.Board">
        SELECT boarId,memberId, title, content, hit, regDt
        FROM board
        WHERE id = #{boardId}
    </select>

    <!-- 게시글 수정 -->
    <update id="update" parameterType="com.buybike.app.domain.Board">
        UPDATE board
        SET memberId = #{memberId},
            title = #{title},
            content = #{content},
            regDt = NOW()
        WHERE boardId = #{boardId}
    </update>

    <!-- 게시글 삭제 -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM board WHERE boardId = #{boardId}
    </delete>


    <select id="getTotalBoardCount" resultType="int">
        SELECT COUNT(*) FROM board
    </select>



    <!-- 페이징 및 정렬된 게시글 목록 조회 -->
    <select id="findAllWithPaging" resultType="com.buybike.app.domain.Board">
        SELECT boardId, memberId,  title, content, hit, created_date
        FROM board
        ORDER BY
            ${sortField} ${sortDir}
        LIMIT #{limit} OFFSET #{offset}
    </select>

</mapper>